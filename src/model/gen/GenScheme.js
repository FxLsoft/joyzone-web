export default function (vm = {}) {
    let _ = this;
    _.id = vm.id || ''; // 主键ID
    _.genTableId = vm.genTableId || ''; // 归属表编号
    _.name = vm.name || ''; // 名称
    _.category = vm.category || ''; // 分类
    _.packageName = vm.packageName || ''; // 生成包路径
    _.moduleName = vm.moduleName || ''; // 生成模块名
    _.subModuleName = vm.subModuleName || '';   // 生成子模块名
    _.functionName = vm.functionName || ''; // 生成功能名
    _.functionSimpleName = vm.functionSimpleName || ''; // 生成功能名（简写）
    _.functionAuthor = vm.functionAuthor || ''; // 生成功能作者
    _.formStyle = vm.formStyle || '';   // 表单风格
    _.projectPath = vm.projectPath || '';   // 生成路径
    _.createBy = vm.createBy || '';         // 创建者
    _.createDate = vm.createDate || '';     // 创建时间
    _.updateBy = vm.updateBy || '';         // 更新者
    _.updateDate = vm.updateDate || '';     // 更新时间
    _.remarks = vm.remarks || '';   // 备注说明
    _.delFlag = vm.delFlag || 0;            // 逻辑删除标记（0：显示；1：隐藏）
}
